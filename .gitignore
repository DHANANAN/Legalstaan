*.iml
.gradle
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# === Secrets — NEVER commit ===
# Release signing keystore (private key for app signing)
*.jks
*.keystore
release-key.jks
keystore.properties

# Firebase config — contains API keys; PUBLIC for the Android client by design,
# but we keep it out of the public repo so anyone wanting to fork must wire up
# their own Firebase project. If you intentionally want to share, remove this line.
app/google-services.json

